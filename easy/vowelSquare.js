export const vowelSquare = (arr) => {

    // // for (let i = 0; i < arr.length - 1; i++) {

    // //     for (let j = 0; arr[0].length - 1; j++) {
    // if ((/[aeiou]/).test(arr[i][j])) {
    //     if ((/[aeiou]/).test(arr[i][j + 1])) {

    //     }
    // }
    // //     }
    // // }

    return arr
}